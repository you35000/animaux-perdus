{% extends 'base.html.twig' %}

{% block title %}Declaration index{% endblock %}

{% block body %}
    <h1>Declaration index</h1>

{#    <table class="table">#}
{#        <thead>#}
{#        <tr>#}
{#            <th>Id</th>#}
{#            <th>DateHeureD</th>#}
{#            <th>InfoSupp</th>#}
{#            <th>actions</th>#}
{#        </tr>#}
{#        </thead>#}
{#        <tbody>#}
        {% for declaration in declarations %}

            <div class="card mb-3" style="max-width: 540px;">
                <div class="row g-0">
                    <div class="col-md-4">
                        <img src="..." class="img-fluid rounded-start" alt="...">
                    </div>
                    <div class="col-md-8">
                        <div class="card-body">
                            <h5 class="card-title">{{ declaration.id }}</h5>
                            <p class="card-text">{{ declaration.infoSupp }}</p>
                            <p class="card-text"><small class="text-muted">{{ declaration.dateHeureD ? declaration.dateHeureD|date('Y-m-d H:i:s') : '' }}</small></p>
                            <a href="{{ path('app_declaration_show', {'id': declaration.id}) }}" class="btn btn-primary">Show</a>
                            <a href="{{ path('app_declaration_edit', {'id': declaration.id}) }}" class="btn btn-primary">Edit</a>
                        </div>
                    </div>
                </div>
            </div>

{#            <tr>#}
{#                <td>{{ declaration.id }}</td>#}
{#                <td>{{ declaration.dateHeureD ? declaration.dateHeureD|date('Y-m-d H:i:s') : '' }}</td>#}
{#                <td>{{ declaration.infoSupp }}</td>#}
{#                <td>#}
{#                    <a href="{{ path('app_declaration_show', {'id': declaration.id}) }}">show</a>#}
{#                    <a href="{{ path('app_declaration_edit', {'id': declaration.id}) }}">edit</a>#}
{#                </td>#}
{#            </tr>#}
        {% else %}
{#            <tr>#}
{#                <td colspan="4">no records found</td>#}
{#            </tr>#}
        {% endfor %}
{#        </tbody>#}
{#    </table>#}

    <a href="{{ path('app_annonce_step_one') }}">Create new</a>
{% endblock %}
